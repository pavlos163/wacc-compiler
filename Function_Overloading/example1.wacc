begin

  int f(int paok) is
    print "x is ";
    println paok;
    paok = 5;
    print "x is now ";
    println paok;
    return paok
  end
  
  int f(int paok, int ok) is 
    print "Function overloading works";
    return 1
  end

  int y = 1;
  print "y is ";
  println y;
  int x = call f(y);
  print "y is still ";
  println y
end
