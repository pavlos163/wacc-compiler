begin

  int f(int paok) is
    print "x is ";
    println paok;
    paok = 5;
    print "x is now ";
    println paok;
    return paok
  end

  int y = 1;
  print "y is ";
  println y;
  int x = call f(y);
  print "y is still ";
  println y
end
